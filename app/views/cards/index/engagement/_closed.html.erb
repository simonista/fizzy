<section id="closed-cards" class="cards cards--closed is-collapsed" style="--card-color: var(--color-ink-light);"
    data-collapsible-columns-target="column">
  <div class="cards__expander btn btn--plain" data-action="click->collapsible-columns#toggle">
    <strong class="font-weight-black cards__expander-count"><%= page.records.count > 99 ? "99+" : page.records.count %></strong>
    <button class="btn btn--plain cards__expander-button txt-uppercase">Done</button>
    <div class="progress progress-<%= [page.records.count, 20].min %>"></div>
  </div>

  <% if page.used? %>
    <%= render partial: "cards/display/preview", collection: page.records, as: :card, cached: true %>

    <% unless page.last? %>
      <div class="full-width">
        <%= cards_next_page_link "closed-cards", page: page, filter: filter, fetch_on_visible: true %>
      </div>
    <% end %>
  <% else %>
    <p class="txt-normal translucent"><%= user_filtering.any? ? "No matches" : "Nothing here" %></p>
  <% end %>
</section>
